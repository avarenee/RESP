import React, { Component } from "react";
import {BrowserRouter as Router, Route, Redirect, Switch} from 'react-router-dom';

export default class FinishCheckIn extends Component {
  render() {
    return(
      <div>
        <h3>Finish Check In</h3>
      </div>
    );
  }
}
